[project]
name = "pdk_symbolic_cells"
version = "0.1.0"
description = "Collection of symbolic cells for Coriolis EDA PDKs."
authors = [ { name = "Coriolis EDA Contributers" } ]
requires-python = ">= 3.8"

[build-system]
requires = [
  "meson",
  "meson-python",
  "ninja",
  "setuptools",
  "cibuildwheel",
  "build",
  "pdm-backend"
]
build-backend = "mesonpy"

[tool.meson]
build-dir = "build"

[tool.cibuildwheel]
build = "cp310-*"
skip = ["cp36-*", "cp37-*", "pp*"]
