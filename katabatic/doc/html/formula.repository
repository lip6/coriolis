\form#0:\[ WL_{max} = width(GCell) \times Htracks(GCell) \]
\form#1:$WL_{max}$
\form#2:\[ Wdensity(depth) = \frac{WL_{used}(depth)}{WL_{max}(depth)} \]
\form#3:\[ Cont_{density} = \frac{|Contacts|}{Htracks \times Vtracks \times 4} \]
\form#4:\[ Htracks = \frac{heigth(GCell)}{Vpitch} + 1 \]
\form#5:$(density(depth),id)$
\form#6:$ rows \times columns $
\form#7:\[ index = c + r \times columns \]
\form#8:\[ row = index / columns \]
\form#9:\[ column = index \div columns \]
